name: 'Generate README.md'

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"
  workflow_run:
    workflows: ["Build And Release"]
    types:
      - completed

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: '^1.21.2'

      - name: Check Go version
        run: go version

      - name: Generate README
        run: |
          git branch
          echo "LATEST_TAG=$(git describe --abbrev=0 --tags --always)" >> "$GITHUB_ENV"
          go build -o bin/jetbrains -gcflags="all=-N -l" -ldflags '-X "github.com/designinlife/jetbrains/common.Version=${LATEST_TAG}" -w -s -extldflags="static"' .
          ./bin/jetbrains ls --readme >README.md
          cat README.md

      - name: Commit README
        uses: EndBug/add-and-commit@v9
        with:
          fetch: false
          message: 'Update README.md'
          add: 'README.md'
